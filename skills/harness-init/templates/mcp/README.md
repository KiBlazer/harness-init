# MCP Integrations

This Harness has no required MCP servers by default.

Use this directory only to document approved external tool integrations and how to access them safely.

Do not store secrets, tokens, credentials, or private endpoint dumps here.
